# =============================================================================
# imdbapi-client — environment template
# Copy to .env and fill in values before running.
# Never commit .env to version control.
# =============================================================================

# --- IMDb API ----------------------------------------------------------------
IMDB_API_KEY=
IMDB_BASE_URL=

# --- Optional: AI agent integration (only needed for examples/langchain_agent_example.py)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
